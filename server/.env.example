# Karuna Server Environment Configuration
# Copy this file to .env and fill in the values

# =============================================================================
# API Keys (Required)
# =============================================================================

# Primary: OpenAI API key for chat and speech-to-text
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Fallback: OpenRouter API key (used if OpenAI fails)
# Get yours at: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-key-here

# =============================================================================
# Authentication (Required for Production)
# =============================================================================

# JWT secret for user authentication
# Generate with: openssl rand -base64 64
JWT_SECRET=generate-a-secure-random-string-here

# JWT secret for admin authentication
# Generate with: openssl rand -base64 64
ADMIN_JWT_SECRET=generate-a-different-secure-random-string-here

# =============================================================================
# Database (Required)
# =============================================================================

# PostgreSQL password (required, no default)
DB_PASSWORD=your-database-password

# PostgreSQL connection settings (optional, defaults shown)
# DB_HOST=localhost
# DB_PORT=5437
# DB_NAME=karuna
# DB_USER=karuna

# =============================================================================
# Server Configuration (Optional)
# =============================================================================

# Server port (default: 3021)
PORT=3021

# Environment (development | production)
NODE_ENV=development

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3020,http://localhost:3000,http://localhost:3030,http://localhost:3040

# =============================================================================
# Development/Debug (Optional)
# =============================================================================

# Enable query logging (true | false)
# DB_LOG_QUERIES=true
